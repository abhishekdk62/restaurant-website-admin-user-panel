<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        /* Modal Background */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }

        /* Modal Content */
        /* Modal Content */
.modal-content {
    background-color: #fff;
    padding: 30px;
    border-radius: 8px;
    width: 90%;
    max-width: 500px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    position: relative;
    max-height: 80vh; /* This limits the height of the modal */
    overflow-y: auto; /* This adds the vertical scrollbar when content exceeds max-height */
}


        /* Close Button */
        .modal .close {
            position: absolute;
            top: 10px;
            right: 10px;
            font-size: 30px;
            color: #888;
            cursor: pointer;
        }

        .modal .close:hover {
            color: #333;
        }

        /* Form Labels and Inputs */
        form label {
            font-size: 1.1em;
            margin-bottom: 5px;
            display: block;
            font-weight: bold;
        }

        form input {
            width: 100%;
            padding: 12px;
            margin-bottom: 15px;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 16px;
        }

        /* Modal Title */
        h2 {
            text-align: center;
            font-size: 1.8em;
            margin-bottom: 20px;
        }

        /* Message for User Not Found */
        p {
            text-align: center;
            font-size: 1.2em;
            color: #555;
        }

        /* Styling for the Email and Password Display */
        .modal-content input[readonly] {
            background-color: #f5f5f5;
            cursor: not-allowed;
        }

    </style>
</head>
<body>

    {{#if users.length}}
  <div id="searchmodal" class="modal" style="display: flex;">
    <div class="modal-content">
      {{#each users}}
        <form>
          <label for="Email">Email</label>
          <input type="email" id="Email" name="Email" value="{{this.email}}" readonly />
          <label for="Password">Password</label>
          <input type="text" id="Password" name="Password" value="{{this.password}}" readonly />
        </form>
      {{/each}}
    </div>
  </div>
{{else}}
  <p>No users found with the provided email prefix</p>
{{/if}}


    <script>
        function closeSearchUserModal() {
            document.getElementById('searchmodal').style.display = 'none';
        }
    </script>
    
</body>
</html>
